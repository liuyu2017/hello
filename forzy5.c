#include <stdio.h>

int main(void)
{ 
    int a = 123456;
    int q, w, e, r, t, y;


    printf("%d\n",a);

    q = a / 100000;
    w = (a % 100000) / 10000;
    e = (a % 100000) % 10000 / 1000;
    r = (a % 100000) % 10000 % 1000 / 100;
    t = (a % 100000) % 10000 % 1000 % 100 / 10;
    y = (a % 100000) % 10000 % 1000 % 100 % 10;

    printf ("%d\n", y * 100000 + t * 10000 + r * 1000 + e * 100 + w * 10+ q);

    return 0;

}
